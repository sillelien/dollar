machine:
  java:
    version: oraclejdk8

general:
  branches:
    ignore:
      - gh-pages

deployment:
  production:
    branch: master
    commands:
        - mvn clean install site scm-publish:publish-scm