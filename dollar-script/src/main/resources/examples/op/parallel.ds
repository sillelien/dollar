const testList := [ TIME(), @@  $1 + 1, {SLEEP(1 S); @@ $1 + 2; TIME();}($1), @@ $1 + 3, TIME() ];
var a= |..| testList("serial: ") ;
var b= |:| testList("parallel: ") ;

//Test different execution orders
.: a[2] >= a[1]
.: b[2] < b[1]
