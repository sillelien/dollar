#!/usr/bin/env dollar


redis := "redis://localhost:6379/test" as uri
send ("Hello World " + date()) to redis

@@ all redis


a := {
    2
    @@ date()
    5
}

@@ a
sleep 0.5
@@ a

//chat := import "github:neilellis:dollar-example-module:chat.ds"
//chat.server( "channel" : "test" )

sleep 1
