a=0

([1,2,3] each { a= $a + $1 })

b= ([1,2,3] *|* { $1 + 1 }) | min

$b <=> 2
$a <=> 6

c = [1,2,3,4] reduce ($1 * $2)
$c <=> 24

d = ["a","b","c","d"] *| {$1 + $2}
$d <=> "abcd"

for loop in [1,2,3,5] {
    @@ $loop
}

