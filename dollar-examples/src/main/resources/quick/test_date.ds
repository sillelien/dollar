@@ DATE()
@@ DATE() + 1
@@ DATE() - 1

.: DATE() + 1 type Date
.: DATE() - 1 type Date
.: DATE() + 1.0 type Date
.: DATE() - 1.0 type Date

.: DATE() + "1.0" type String
.: DATE() / "1.0" type Decimal

.: DATE() * 10 type Decimal
.: DATE() / 10 type Decimal

@@ DATE().DAY_OF_WEEK

@@ DATE()['DAY_OF_YEAR']=1

def Fortnight {$1 * 14}

@@ DATE() + 1 DAY
@@ DATE() + 1 HOUR
@@ DATE() + 1 SEC
@@ DATE() + 1 Fortnight





