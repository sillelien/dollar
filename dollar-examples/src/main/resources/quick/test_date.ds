@@ DATE()
@@ DATE() + 1
@@ DATE() - 1

.: DATE() + 1 is Date
.: DATE() - 1 is Date
.: DATE() + 1.0 is Date
.: DATE() - 1.0 is Date

.: DATE() + "1.0" is String
.: DATE() / "1.0" is Decimal

.: DATE() * 10 is Decimal
.: DATE() / 10 is Decimal

@@ DATE().DAY_OF_WEEK

@@ DATE()['DAY_OF_YEAR']=1

def Fortnight {$1 * 14}

@@ DATE() + 1 DAY
@@ DATE() + 1 HOUR
@@ DATE() + 1 SEC
@@ DATE() + 1 Fortnight





