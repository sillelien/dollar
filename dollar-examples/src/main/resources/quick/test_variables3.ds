
//Variable constraints
def string (it type String)
var (string) d="test"

var <String> f="testing"


var (previous != it || previous type Void) e = 1
@@e
e=2
e=3

var errorOccured= false

&{
    error { @@ "Caught " + errorClass ; errorOccured= true}
    @@ "*******"
    e=3
}

