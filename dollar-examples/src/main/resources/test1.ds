const f={1;2;3;4}
@@ f

const multiline={
"Hello "
"World"
}
const c={ "test2": 1}

@@ multiline
@@ c
@@ c.test2

var a="Hello";
const b="World";

@@ a;

a="Goodbye";
const d= void | 2;

//Assert d == 2

.: d == 2

@@ d == 2 ? "It's True @@!";

@@ d;

@@ a;
@@ c ;

@@ [a, b, c] ;

def colorEnum { it in ["red","green","blue"] }

//Use it as a constraint
var (colorEnum) myColor= "green"

//@@ {"test" : 1, "test2" : 2} | 'mvn:dollar.internal.runtime.script.TestPipe:com.sillelien:dollar-script:0-SNAPSHOT'

def greaterThan3 {it > 3};
def greaterThan2 {it > 2};

var (greaterThan3) x = 5;

var (greaterThan3) y = 25;
var (greaterThan2) z = 25;

//z=0;
if(false) {
    error { @@ "Error"}
    x= y;
    x= z;
    x= 50;
}
