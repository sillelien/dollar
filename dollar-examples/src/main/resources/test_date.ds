@@ DATE()
@@ DATE() + 1
@@ DATE() - 1

.: DATE() + 1 is Date
.: DATE() - 1 is Date
.: DATE() + 1.0 is Date
.: DATE() - 1.0 is Date

.: DATE() + "1.0" is Date
.: DATE() / "1.0" is Decimal

.: DATE() * 10 is Decimal
.: DATE() / 10 is Decimal

@@ DATE().DAY_OF_WEEK

@@ DATE()['DAY_OF_YEAR']=1

Fortnight := ($1 * 14)

@@ DATE() + 1 Day
@@ DATE() + 1 Hour
@@ DATE() + 1 Sec
@@ DATE() + 1 Fortnight


[1,2,3,4] -> SORT -> MIN
[1,2,3,4] SORT MIN



